import React from "react";
import { ObservedBehaviorsScreen } from "@/features/analysis/screens/observed-behaviors-screen";

/** Route for a student's observed behaviors analysis. */
export default function ObservedBehaviorsRoute() {
  return <ObservedBehaviorsScreen />;
}
