import React from "react";
import { HelpRecordsScreen } from "@/features/analysis/screens/help-records-screen";

/** Route for a student's help-level records analysis. */
export default function HelpRecordsRoute() {
  return <HelpRecordsScreen />;
}
