import { StudentReportsScreen } from "@/features/reports/screens/student-reports-screen";

/** Route for a single student's reports list. */
export default function StudentReportsRoute() {
  return <StudentReportsScreen />;
}
