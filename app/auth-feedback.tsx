import { FeedbackScreen } from "../features/auth/screens/feedback-screen";
import React from "react";

/** Route for the post-auth feedback screen (success / pending approval). */
export default function FeedbackRoute() {
  return <FeedbackScreen />;
}
