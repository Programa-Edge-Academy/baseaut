import React from "react";
import { TeamScreen } from "../features/teams/screens/team-screen";

/** Route for the team management screen. */
export default function TeamRoute() {
  return <TeamScreen />;
}